# 百首異聞 開発基準

## 第1ゴール

「百首異聞・平安編の縦切り試作版を、10〜15分遊べる状態にする」

### 完成条件

- TOPの「百首異聞」から入れる
- 怪異に決まり字3枚が表示される
- プレイヤーが下の句札5枚をセットする
- 札のテーマでリンク効果が発動する
- HP・MPを使うターン戦闘が成立する
- 正解札は一撃、その他の札は通常ダメージになる
- 敵撃破で歌のカケラを獲得し、札がレベルアップする
- 10体の怪異と平安編ボス1体の描写が完成している
- 一周では10体から4体が選ばれ、最後にボスと戦う
- タイトルからボス撃破まで進行できる
- セーブして再開できる
- 重大エラーがなく、自動テストを通過する

## 体験の優先順位

1. 決まり字から下の句を思い出し、一撃破断する気持ちよさ
2. 5枚の組み合わせを考え、複数の共鳴を作る楽しさ
3. 怪異を倒して札が育ち、次も触りたくなる継続感
4. 小学生でも説明を読み続けず操作できる明快さ

抽象的な判断はこの順位を基準に行います。根幹を変える変更、既存トレーニング機能を壊す変更、復元困難な変更のみ確認対象とします。

## UI/UX品質基準

- 主要操作のタップ領域は44px以上
- 重要文字は本文相当以上とし、9px以下を主要情報に使わない
- 選択、決定、攻撃、被弾、討伐に視覚・音・触覚の反応を返す
- 1画面の主要目的を一つに絞る
- 札選択は「今回の候補」と「全100首」を切り替えられる
- `prefers-reduced-motion` とゲーム内の演出軽減設定に対応する
- 既存の100首下の句データとトレーニング機能を変更せず利用する
- モバイル縦画面を主対象とし、430〜520px幅で最適化する

## 怪異レベル3基準

各怪異は以下をすべて持ちます。

- 一目で区別できる固有シルエット
- 固有の配色、素材感、発光部
- 呼吸または浮遊モーション
- その怪異が存在する場所と登場文
- 決まり字3枚を視認できる配置
- 攻撃、被弾、一撃破断、討伐演出
- 怪異図録に残る名前、異名、説明

## アーキテクチャ

- `rpg-data.js`: 調整可能な章・怪異・札タグのデータ
- `rpg-core.js`: DOMに依存しない戦闘計算とセーブ補正
- `rpg-art.js`: 外部画像に依存しないSVG怪異描画
- `hyakushu-ibun.js`: 画面状態とユーザー操作
- `hyakushu-ibun.css`: モバイル向け表現・演出

旧 `rpg*.html` は比較資料として残します。新しい本編へ機能を追加するときは、旧試作への後付けスクリプトではなく上記4層へ統合します。

## 自動テスト

- `tests/rpg-core.test.js`: 10怪異、3決まり字、共鳴、戦闘、育成、セーブ
- `tests/rpg-assets.test.js`: TOP導線とアセット参照
- `tests/rpg-smoke.test.js`: タイトルから4怪異＋ボス撃破、クリア保存まで

機能変更後は `npm test` と `npm run lint` を通してから公開します。

## 次の改良候補

1. 実端末プレイ時間と迷い箇所の計測
2. 10体すべての遭遇テストと固有演出の追加
3. 札選択候補の難易度調整
4. 共鳴の演出差と戦術差の拡張
5. カケラ獲得後のレベルアップ選択画面
6. 平安編のイベント会話と分岐
